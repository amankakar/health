import React from 'react';

export default class HideDiv extends React.Component {
	constructor(props) {
		super(props);

	}

	render() {
		if(this.props.hide) {
			return false;
		}
		return <div>{this.props.children}</div>;
	}

}

HideDiv.Proptypes = {
	hide: React.PropTypes.bool
}
